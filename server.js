module.exports = {
    mute_id: function (user,userID,channelID,message,event){
        // debugger;
        //   bot.mute({server: bot.serverFromChannel(channelID), user:userID,role:"Bot"}, function(err, res){
        //     bot.sendMessage({to: channelID, message: "<@" + userID + "> Unable to mute"});
        //   });
      }
  };
